{"app_summary":"This application is a trading simulation platform allowing users to manage accounts, deposit/withdraw funds, trade shares and cryptocurrencies, and track their portfolio value and performance over time. It handles transactions with rules to ensure account consistency and provides interfaces to retrieve current market prices for stocks and cryptocurrencies.","modules":[{"name":"backend.py","responsibility":"Manage all trading logic, such as account creation, fund management, and transaction records.","interfaces":["def create_account(user_id: str) -> None","def deposit_funds(user_id: str, amount: float) -> None","def withdraw_funds(user_id: str, amount: float) -> None","def buy_asset(user_id: str, asset_symbol: str, quantity: float) -> None","def sell_asset(user_id: str, asset_symbol: str, quantity: float) -> None","def get_portfolio_value(user_id: str) -> float","def get_profit_loss(user_id: str) -> float","def get_holdings(user_id: str) -> dict","def list_transactions(user_id: str) -> list"],"dependencies":["main.py"]},{"name":"app.py","responsibility":"Provide the frontend interface for user interaction.","interfaces":["def main() -> None"],"dependencies":["backend.py","main.py"]},{"name":"main.py","responsibility":"Auxiliary functions for price retrievals and managing integration with external resources.","interfaces":["def get_share_price(symbol: str) -> float","def get_crypto_price(symbol: str) -> float"],"dependencies":[]}],"integration_sequence":["Initialize all necessary services in main.py, including mock integrations for price retrievals.","Implement backend.py functions ensuring all internal logic checks for balance and transaction validity.","Use interfaces provided by backend.py in app.py to handle user requests.","Validate integration by running test cases matching acceptance criteria in a controlled environment."],"acceptance_criteria":["Users can successfully create accounts, and logs are maintained.","Deposits and withdrawals reflect correctly in the user balance, adhering to limits.","Buying and selling transactions are executed only when account checks are passed.","User's portfolio value calculation matches expected results according to market prices.","Portfolio holdings and transaction histories are accurately retrieved.","Application reflects real market prices using mocked functions.","Integration tests confirm successful wiring and functionality from app.py entrypoint."]}